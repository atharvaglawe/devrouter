package constant

// Grouped const block (mirrors the real adclickroute constant pkg),
// so the string-const extractor must handle grouped declarations.
const (
	DefaultClickTransferDomain = "clks.example.net"
	DefaultPath                = "/trf"
)
